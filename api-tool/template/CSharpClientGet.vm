    public static async $retType $methodName($params)
    {
        return await Get<$retType>("$path", q => {
        #foreach($queryParam in $queryParams)
            q.Set("$queryParam.key", $queryParam.value);
        #end
        });
    }

